# hellhunter.web

Static website for my book. This repository will be published to GitHub; Cloudflare Pages can deploy the site from the repository.

To publish:

1. Create a GitHub repository.
2. Add the remote: git remote add origin <git-url>
3. Push: git push -u origin main
